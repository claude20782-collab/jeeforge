'use client'
// STUB — owned by agent G. Replace with full implementation. Keep the export name: AdminView
export function AdminView(props: unknown) {
  return <div className="p-8 text-center text-muted-foreground">AdminView loading…</div>
}
